# Legacy Data

This file documents codes, URNs, and heuristics that are no longer used by the recommender. it is kept for historical reference since some of this may show in db data.

## Removed Custom Code URNs

- `kks-alm`
  - Was used to mark independent courses.
  - Replaced by `courseUnitRealisationTypeUrn` containing `independent`.
